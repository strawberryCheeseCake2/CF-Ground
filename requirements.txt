# Minimal runtime requirements for CF-Ground inside Docker
# NOTE: PyTorch is installed in the Dockerfile with the proper CUDA/CPU index-url.

# Core
transformers==4.49.0
qwen-vl-utils
numpy<2.0
pandas
matplotlib
pillow
safetensors
accelerate==1.8.1
bitsandbytes==0.43.1
scikit-image
opencv-python
tqdm

# Optional/CLI/Logging tools (safe to keep)
wandb
